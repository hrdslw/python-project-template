name: CI

on:
  push:
    branches: [main, Linter, Paths, ProjectPackage]

jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - run: uv lock --locked
      - run: uv sync --all-groups
      - run: uv run ruff check .